const Store = $state<{population: number, workers: number, soldiers: number, merchants: number, smiths: number, priests: number}>({population: 300, workers: 6, soldiers: 4, merchants: 1, smiths: 1, priests: 0});

export default Store;